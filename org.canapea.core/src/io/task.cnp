module "canapea/io/task"
  exposing
    | Task
    | attempt


# TODO: `Task` needs to work asap
type Task =
  | Task


let attempt : caps, ((Task -> Result a err) -> Result b err) -> Result b err
let attempt capabilities fn =
  debug.todo "notimplemented"
